# Jetson Xavier specific requirements
# Use this file instead of requirements.txt on Jetson devices

transformers>=4.30.0
torch>=2.0.0
torchvision>=0.15.0
Pillow>=9.0.0
scipy
opencv-python-headless>=4.8.0
# YOLO support (optional)
ultralytics>=8.0.0
numpy>=1.21.0

# Jetson-specific fixes:
# Install polars without problematic backend dependencies
polars --no-deps
# Alternative: use pandas instead of polars if polars fails
# pandas>=1.5.0
